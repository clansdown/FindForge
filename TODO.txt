* Add the ability to copy the raw synthesis markdown

* Add a feature to re-run the synthesis stage with an on-the-fly edited synthesis system prompt

* Add the ability to clear individual settings back to default, especially system prompts.

* Add the ability to have multiple system prompts (the default might be unremovable from the list to accomplish resetting to default)

* card catalog functionality (reading many documents and characterizing them according to characteristics the user picks for the card catalog)

* add the ability to copy code samples and other block quotes in the markdown rather than the whole markdown
